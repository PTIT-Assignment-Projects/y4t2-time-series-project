Viewed data_repo.py:232-322

Dưới đây là giải thích chi tiết từng dòng code trong hàm [fetch_macro](cci:1://file:///home/duongvct/Documents/workspace/PTIT/Y4T2/FX_BOT/04.deployment_automation/scripts/data_repo.py:231:2-321:23), nơi tác giả xử lý dữ liệu vĩ mô từ FRED:

### 1. Thiết lập thời gian (Lines 232-237)
*   **234-235**: Nếu không truyền vào ngày bắt đầu (`min_date`), bot sẽ lấy mặc định từ ngày 27/08/2023.
*   **237**: Chuyển chuỗi ký tự ngày thành định dạng `datetime` của Pandas để tính toán.

### 2. Thu thập dữ liệu thô và tính toán tăng trưởng (Lines 240-264)
Trong đoạn này, tác giả dùng `pdr.DataReader` để lấy dữ liệu từ hệ thống FRED. `time.sleep(1)` được dùng để tránh việc gửi yêu cầu quá nhanh làm API bị khóa.

*   **240-242 (GDP - Theo Quý)**: Lấy GDP tiềm năng. Tính tăng trưởng so với năm ngoái (`shift(4)` vì 1 năm có 4 quý) và so với quý trước (`shift(1)`).
*   **245-247 (CPI - Theo Tháng)**: Lấy chỉ số lạm phát lõi. Tính tăng trưởng năm (`shift(12)` vì 1 năm có 12 tháng) và tăng trưởng tháng (`shift(1)`).
*   **250-252 (Trade Balance - Theo Tháng)**: Lấy cán cân thương mại và tính mức độ thay đổi YoY/MoM.
*   **254 (Fed Funds)**: Lấy lãi suất liên bang Mỹ.
*   **257-263 (Bond Yields - Theo Ngày)**: Lấy lợi suất trái phiếu chính phủ Mỹ kỳ hạn 1 năm, 5 năm và 10 năm.

### 3. Chuẩn bị bảng tạm để gộp (Lines 266-268)
*   Tác giả chỉ lọc lấy các cột tỉ lệ tăng trưởng đã tính toán để chuẩn bị ghép vào bảng chính, bỏ qua các giá trị thô để giảm nhiễu.

### 4. Logic gộp dữ liệu (Merging) - Phần khó nhất (Lines 271-316)
Vì các chỉ số có tần suất khác nhau (Ngày, Tháng, Quý), tác giả dùng kỹ thuật "Gắn nhãn thời gian chung":

*   **271-276**: Bắt đầu bằng việc gộp 2 khung dữ liệu hàng ngày (Trái phiếu 1 năm và 5 năm) lại với nhau.
*   **279**: Tạo cột `Quarter`. Ví dụ: Tất cả các ngày trong Quý 1/2024 (từ 01/01 đến 31/03) đều được gán nhãn là `2024-01-01`.
*   **281-286**: Dùng nhãn `Quarter` này để ghép dữ liệu GDP (vốn chỉ có 1 giá trị mỗi quý) vào từng ngày tương ứng.
*   **288**: Tạo cột `Month`. Ví dụ: Tất cả các ngày trong tháng 3 đều được gán nhãn là `2024-03-01`.
*   **290-309**: Dùng nhãn `Month` này để ghép CPI, Cán cân thương mại và Lãi suất FED vào bảng dữ liệu hàng ngày.
*   **311-316**: Ghép nốt lợi suất trái phiếu 10 năm vào theo đúng ngày (`Date`).

### 5. Xử lý dữ liệu trống (Forward Fill) (Lines 318-320)
*   **Vấn đề**: Sau khi ghép, chỉ có ngày đầu tiên của tháng/quý là có dữ liệu GDP hay CPI, các ngày còn lại sẽ bị trống (`NaN`).
*   **Giải pháp (`ffill()`)**: Lấy giá trị của ngày có dữ liệu gần nhất để điền vào các ô trống phía sau. Điều này giả định rằng chỉ số kinh tế (như GDP) sẽ được giữ nguyên cho đến khi có báo cáo mới của quý sau.

### 6. Lưu kết quả (Line 322)
*   Gán bảng dữ liệu hoàn chỉnh đã được làm sạch và điền đầy đủ vào biến `self.macro_df`.

**Kết quả cuối cùng:** Bạn có một bảng dữ liệu hàng ngày, trong đó mỗi ngày đều biết được lãi suất là bao nhiêu, lợi suất trái phiếu thế nào và tình hình lạm phát/tăng trưởng kinh tế tại thời điểm đó ra sao.